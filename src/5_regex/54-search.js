// search for a substring, return
// it's position in the string (0-based)
let inStr = 'thou shalt not steal';
const position = inStr.search(/not/i);
console.log('position :', position);
// output:
// position : 11
